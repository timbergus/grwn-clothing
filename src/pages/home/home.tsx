import './home.scss';

import Directory from '../../components/directory/directory';

const Home = () => {
  return (
    <div className="home">
      <Directory />
    </div>
  );
};

export default Home;
